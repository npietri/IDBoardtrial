import { createUseStyles } from 'react-jss';

export default createUseStyles({
    container: {
        backgroundColor: '#999999',
        width: '100%',

    },
    containerContent: {
        padding: 32,
        minHeight: "600px"
    }
});
