import { createUseStyles } from 'react-jss';

export default createUseStyles({
    mainContainer: {
        display: 'flex',
    },
    contentContainer: {
        display: 'flex',
        border: '2px solid red',
        flexGrow: 1,
    },
});
