import React from 'react';
import styles from './style';

const Configuration = () => {
    const classes = styles();
    return <div className={classes.text}>Configuration</div>;
};

export default Configuration;
