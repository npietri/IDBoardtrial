import { createUseStyles } from 'react-jss';

export default createUseStyles({
    container: {
        display: 'flex',
        width: '100%',
    },
    sidebarContainer: {
        overflow: 'hidden',
        minWidth: 180,
        minHeight: 1080,
    },
});
