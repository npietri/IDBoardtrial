import { createUseStyles } from 'react-jss';

export default createUseStyles({
    container: {
        textAlign: 'center',
    },
    errorText: {
        backgroundColor: '#222',
        fontSize: 72,
        fontWeight: 'bold',
        fontFamily: 'impact',
        color: '#FFF',
    },
});
