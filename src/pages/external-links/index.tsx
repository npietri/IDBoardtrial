import React from 'react';
import styles from './style';

const ExternalLinks = () => {
    const classes = styles();
    return <div className={classes.text}>ExternalLinks</div>;
};

export default ExternalLinks;
